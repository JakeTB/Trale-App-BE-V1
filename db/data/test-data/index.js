const users = require("./users");
const pubs = require("./pubs");
const routes = require("./routes");
const testData = {
  userData: users,
  routesData: routes,
  pubsData: pubs
};
module.exports = testData;
